I will update the Admin Dashboard License page to match your requested format and ensure the backend provides the necessary data (Device Model, License Key, Activation Date).

### **Plan of Action**

1.  **Update Backend (`src/services/licenseService.js`)**
    *   **Capture Metadata:** Modify `activateLicense()` to save the **License Key** and **Activation Date** to `license.json` upon successful activation.
    *   **Load Metadata:** Update `loadLicense()` to read these saved fields and attach them to the license data object.
    *   **Fetch Device Model:** Integrate `HardwareService` to fetch the device model (e.g., "Raspberry Pi 4") on startup and expose it in `getStatus()`.

2.  **Update Frontend (`public/admin.html`)**
    *   **Redesign UI:** Rewrite the "System License" card HTML to match your specified layout:
        *   **Status:** Active/Inactive
        *   **HWID:** (Existing)
        *   **Device Model:** (New field)
        *   **Key:** (Always shown)
        *   **Activated:** (Date or "Not Activated")
        *   **Expiration:** ("No Expiration" for active licenses, or trial days remaining)
    *   **Update Logic:** Modify `loadLicenseInfo()` to populate these new fields from the API response.

### **Verification**
*   **Fresh Install:** Will show "Trial Mode", no key, no activation date.
*   **After Activation:** Will show "Active", the entered Key, the Activation Date, and "No Expiration".
